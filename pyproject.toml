[project]
name = "nano-infer-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ito-integral", email = "dmzjm@outlook.com" }
]
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.4",
    "safetensors>=0.8.0",
    "tokenizers>=0.23.1",
    "torch==2.11.0",
]

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.15.22",
]

[[tool.uv.index]]
url = "https://download.pytorch.org/whl/cu128"
